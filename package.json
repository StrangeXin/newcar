{
  "name": "newcar",
  "private": true,
  "packageManager": "npm@10.9.3",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "npm --workspace apps/api run test",
    "test:e2e": "playwright test",
    "test:chat:multi-turn": "tsx scripts/test-chat-multi-turn.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2",
    "turbo": "^2.5.0"
  },
  "dependencies": {
    "@langchain/openai": "^0.6.17"
  }
}
